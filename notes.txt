github 

1.git clone (url=link)
2.git status (finding a changes file)
3.git add *(*=all/ file name)
4.git commit -m "comment"
5.git cofig --global user.name"user name"
6.git config --global user.email"user email"
7.git push -u origin main (main=branch name)
